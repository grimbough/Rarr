% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_metadata.R
\name{zarr_consolidate_metadata}
\alias{zarr_consolidate_metadata}
\title{Consolidate Zarr metadata files into a single file}
\usage{
zarr_consolidate_metadata(
  zarr_store_path,
  s3_client = NULL,
  action = c("write", "return")
)
}
\arguments{
\item{zarr_store_path}{A character vector of length 1. This provides the path to a Zarr store.}

\item{s3_client}{A list representing an S3 client.  This should be produced
by \code{\link[paws.storage:s3]{paws.storage::s3()}}.}

\item{action}{A character string specifying the action to take with the consolidated metadata.
If \code{"write"} (the default), the consolidated metadata will be written back to the Zarr store.
If \code{"return"}, the consolidated metadata will be returned as a list without writing it back to the store.
The latter is particularly useful for non-writable stores.}
}
\value{
If \code{action} is \code{"return"}, a list containing the consolidated metadata.
Otherwise, the function is called for its side effect and \code{NULL} is returned invisibly.
}
\description{
This function reads all the metadata files in a Zarr store and consolidates them into a single file.
Thanks to this, a single request can be made to retrieve all the elements and their related metadata for a Zarr store,
which is especially beneficial for remote stores like S3.
}
