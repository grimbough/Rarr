# Subset extraction for an array with a variable number of dimensions.

Subset extraction for an array with a variable number of dimensions.

## Usage

``` r
.extract_chunk(x, idx)
```

## Arguments

- x:

  Array to extract from.

- idx:

  List of index vectors, one per dimension.

## Value

The extracted sub-array (with `drop = FALSE`).
